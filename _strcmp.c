#include "simple_shell.h"

/**
 * _strcmp - copy en fonction de n
 * @s1: variable
 * @s2: variable
 * Return: (dest)
 */
int _strcmp(char *s1, char *s2)
{
	int i = 0:

		while
			(s1[i] == s2[i] && s1[i] && s2[i])
			{
				i++;
			}

	return (s1[i] - s2[i]);
}
